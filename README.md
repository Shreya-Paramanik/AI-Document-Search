The AI-Document Search Project is a project build using the RAG(Retrieval Augmentation Generation) pipeline.
- It lets you upload pdfs files only.
- You can then ask questions from your pdf
- Uses Google's gemini as the LLM.
